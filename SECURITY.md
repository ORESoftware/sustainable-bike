# Security and safety reporting

Do not publish exploitable battery unlock methods, controller bypasses, signing keys, or private credentials in an issue. Report security-sensitive findings privately to the repository owner through GitHub's private vulnerability reporting feature when enabled.

Treat overspeed control, brake-cut logic, battery management, charger authentication, firmware update signing, hub-retention tamper detection, and serialized service records as security-relevant safety controls.
